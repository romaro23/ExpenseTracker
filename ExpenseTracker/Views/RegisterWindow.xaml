<Window x:Class="ExpenseTracker.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExpenseTracker.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Реєстрація" Width="350" Height="300">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Новий логін:"/>
            <TextBox Text="{Binding Username}" Margin="0,0,0,15"/>
            <TextBlock Text="Новий пароль:"/>
            <PasswordBox Margin="0,0,0,15"
                         materialDesign:PasswordBoxAssist.Password="{Binding Password, Mode=TwoWay}"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
            />
            <Button CommandParameter="dummy" Content="Зареєструватися" Command="{Binding RegisterCommand}" Margin="0,0,0,10"/>
            <Button CommandParameter="dummy" Content="Увійти" Command="{Binding LoginCommand}" 
                    />
        </StackPanel>
    </Grid>
</Window>
